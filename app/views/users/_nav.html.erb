<header class="container">
<div class="nav_container">
    <h1>Recipes App</h1>
    <nav>
        <ul class="options_container">
            <% unless user_signed_in? %>
                <li>
                    <%= link_to "Log in", new_user_session_path %>
                </li>
            <% end %>
            <% if user_signed_in? %>
            <li>
                <%= link_to "Foods", foods_path, class: "nav_options" %>
            </li>
            <li>
                <%= link_to "Recipes", recipes_path, class: "nav_options" %>
            </li>
            <li>
                <%= link_to "Public Recipes", root_path, class: "nav_options" %>
            </li>
                <li>
                    <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav_options" %>
                </li>
                <li>
                    <button class="btn btn-outline-success">Hello <%= current_user.name %></button>
                </li>
                <% end %>
        </ul>
    </nav>
    </div>
</header>
